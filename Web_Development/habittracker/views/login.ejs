<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Habit Tracker - Login</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f5f5f5;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    .login-container {
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      width: 300px;
      max-width: 100%;
      text-align: center;
      padding: 20px;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      color: #555;
      font-weight: bold;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-bottom: 16px;
      box-sizing: border-box;
      border: 1px solid #ccc;
      border-radius: 4px;
      transition: border-color 0.3s;
    }

    input:focus {
      border-color: #3498db; /* Ändere die Fokusfarbe auf Blau */
    }

    button {
      background-color: #3498db; /* Ändere die Hintergrundfarbe des Buttons auf Blau */
      color: #fff;
      padding: 10px 15px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #2980b9; /* Ändere die Hover-Hintergrundfarbe des Buttons auf Dunkelblau */
    }

    a {
      display: block;
      text-align: center;
      margin-top: 10px;
      color: #3498db; /* Ändere die Linkfarbe auf Blau */
      text-decoration: none;
      transition: color 0.3s;
    }

    a:hover {
      color: #2980b9; /* Ändere die Hover-Linkfarbe auf Dunkelblau */
    }

    .error {
      color: #ff0000;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Habit Tracker - Login</h1>
    <% if (messages.error) {%>
      <p class="error"><%= messages.error %></p>
    <% } %>
    <form action="/login" method="POST">
      <div>
        <label for="email">Email</label>
        <input type="email" id="email" name="email" required>
      </div>
      <div>
        <label for="password">Password</label>
        <input type="password" id="password" name="password" required>
      </div>
      <button type="submit">Login</button>
    </form>
    <a href="/register">Noch kein Konto? Registrieren</a>
  </div>
</body>
</html>
